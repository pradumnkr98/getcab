<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ashish.justgetit">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login_page" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_apikey" />

        <activity android:name=".homepage1" />
        <activity android:name=".registerhere" />
        <activity android:name=".forgetpassword" />
        <activity android:name=".current_duty" />
        <activity android:name=".available_booking" />
        <activity android:name=".future_duties" />
        <activity android:name=".account_details" />
        <activity android:name=".driver_incentives" />
        <activity android:name=".profile" />
        <activity android:name=".recharge" />
        <activity android:name=".OTP_confirmation" />
        <activity android:name=".create_newpassword" />
        <activity
            android:name=".outstation"
            android:label="@string/title_activity_outstation"
            android:theme="@style/AppTheme" />
        <activity android:name=".MainActivity" />
        <activity android:name=".final_booking" />
        <activity android:name=".profile_page" />
        <activity android:name=".booking_summary" />
        <activity android:name=".getting_nearby_driver" />
        <activity android:name=".outstation_roundway" />
        <activity android:name=".roundway_finalbooking" />
        <activity android:name=".roundway_bookingsummary" />
        <activity android:name=".roundway_gettingnearbydriver"></activity>
    </application>

</manifest>