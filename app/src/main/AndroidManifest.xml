<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ashish.justgetit">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@drawable/justcablogo"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/justcablogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login_page" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_apikey" />

        <activity android:name=".homepage1" />
        <activity android:name=".registerhere" />
        <activity android:name=".forgetpassword" />
        <activity android:name=".navigation_drawer.current_ride" />
        <activity android:name=".navigation_drawer.future_ride" />
        <activity android:name=".future_duties" />
        <activity android:name=".navigation_drawer.settings" />
        <activity android:name=".navigation_drawer.wallet" />
        <activity android:name=".OTP_confirmation" />
        <activity android:name=".create_newpassword" />
        <activity android:name=".outstation_one_way.oneway_finalbooking" />
        <activity android:name=".oneway_bookingsummary" />
        <activity android:name=".local_booking.final_booking" />
        <activity android:name=".navigation_drawer.profile_page" />
        <activity android:name=".local_booking.booking_summary" />
        <activity android:name=".outstation.outstation_roundway" />
        <activity android:name=".outstation.roundway_finalbooking" />
        <activity android:name=".outstation.roundway_bookingsummary" />
        <activity android:name=".outstation.roundway_gettingnearbydriver" />
        <activity android:name=".local_booking.getting_nearby_driver" />
        <activity android:name=".outstation_one_way.outstation_one_way" />
        <activity android:name=".outstation_one_way.oneway_bookingsummary" />
        <activity android:name=".navigation_drawer.Edit_Account" />
        <activity android:name=".navigation_drawer.completed_ride" />
        <activity
            android:name=".navigation_drawer.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>